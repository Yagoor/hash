{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Current",
            "type": "shell",
            "command": "make DEBUG=1 -C '${workspaceFolder}/build' ${fileBasenameNoExtension}.test",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },    
    ]
}